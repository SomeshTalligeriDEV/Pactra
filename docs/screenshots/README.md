# Screenshots

Referenced from the top-level `README.md`. Drop PNGs in here with these exact
names and they'll render there automatically — no README edit needed beyond
what's already wired in.

| File | What it shows |
|---|---|
| `console-overview.png` | Console → Overview: a signed mandate, its budget/lifetime/tranche figures, live headroom |
| `console-new-mandate.png` | Console → New mandate: the six-field signing form |
| `console-agents.png` | Console → Agents: the delegation tree, a spawned child narrower than its parent |
| `site-landing.png` | The public site's hero |
| `refusal-page.png` | A published refusal, `/refusal/:id`, naming the transaction that produced it |
| `purchase-flow.png` | A `pactra_fetch` call's JSON response: `draw.released`, the settlement tx |

1280×800 or larger, PNG. Crop to the browser viewport — no OS chrome, no bookmarks bar.
